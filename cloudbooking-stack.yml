nginx:
  image: nginx:alpine
  ports:
    - "8080:8080"
  volumes:
    - ./nginx.conf:/etc/nginx/nginx.conf:ro
    - ./web:/usr/share/nginx/html:ro
  depends_on:
    - app
  deploy:
    replicas: 1
    restart_policy:
      condition: on-failure
  networks:
    - cloudbooking_net
